apiVersion: v1
kind: Service
metadata:
  name: nest-app-service
spec:
  type: NodePort
  selector:
    app: nest-js
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30002